name: Display message on Push

on:
    push:
    
jobs:
    show-message:
        runs-on: ubuntu-latest

        steps:
            - name: Print Message
              run: echo "Push detected. CI is running!"